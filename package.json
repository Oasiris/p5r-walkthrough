{
  "name": "p5r-walkthrough",
  "version": "1.1.1d1",
  "description": "Max-confidants walkthrough for Persona 5 Royal",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "pack": "npm run clean && ./pack_html.sh 1.1.1d1",
    "start": "live-server dist --open=ace-walkthrough.html",
    "serve": "npm run pack && npm run start"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "showdown": "^1.9.1"
  }
}
